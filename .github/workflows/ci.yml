name: CI

on:
  workflow_dispatch:

jobs:
  info:
    name: CI disabled while Pages deploy is stabilized
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "CI is temporarily manual-only. Use Deploy to GitHub Pages for build and deployment checks."
